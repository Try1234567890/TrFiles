package me.tr.configuration.file;

import me.tr.configuration.memory.MemoryConfiguration;
import me.tr.configuration.memory.MemoryOptions;

public class FileOptions extends MemoryOptions {

    protected FileOptions(MemoryConfiguration configuration) {
        super(configuration);
    }
}
